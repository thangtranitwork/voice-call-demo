# Server
SERVER_PORT=8080
ENV=development

# Database (MySQL in Docker - Port changed to 3307)
DB_HOST=localhost
DB_PORT=3307
DB_USER=user
DB_PASSWORD=secret_password
DB_NAME=voice_call_demo

# Redis (in Docker)
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=

# JWT
JWT_SECRET=your-super-secret-key
JWT_ACCESS_EXPIRE=15m
JWT_REFRESH_EXPIRE=168h

# CORS
ALLOWED_ORIGINS=http://localhost:3000
